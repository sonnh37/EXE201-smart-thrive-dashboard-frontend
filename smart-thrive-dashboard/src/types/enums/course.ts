export enum CourseType {
  Online = 1,
  Offline = 2,
}

export enum CourseStatus {
  Active = 1,
  Inactive = 2,
  Completed = 3,
}

export enum DayInWeek {
  Monday = 1,
  Tuesday = 2,
  Wednesday = 3,
}
